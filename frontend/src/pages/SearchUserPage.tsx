import React from "react";
import SearchUserTemplate from "../components/templates/SearchUserTemplate";

const SearchUserPage: React.FC = () => {
  return <SearchUserTemplate />;
};

export default SearchUserPage;
