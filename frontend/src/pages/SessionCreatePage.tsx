import React from "react";
import SessionCreateTemplate from "../components/templates/SessionCreateTemplate";

const SessionCreatePage: React.FC = () => {
  return (
    <main>
      <SessionCreateTemplate />
    </main>
  );
};

export default SessionCreatePage;
